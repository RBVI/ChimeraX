--- tcia_utils.orig/nbia.py	2026-04-06 11:51:39
+++ tcia_utils/nbia.py	2026-04-06 12:43:07
@@ -1,9 +1,9 @@
 ####### setup
+from __future__ import annotations
 from typing import Union, List, Optional
 import logging
 import warnings
 import requests
-import pandas as pd
 import getpass
 import zipfile
 import io
@@ -13,13 +13,6 @@
 from datetime import datetime
 from datetime import timedelta
 from enum import Enum
-import plotly.express as px
-from tcia_utils.utils import searchDf
-from tcia_utils.utils import copy_df_cols
-from tcia_utils.utils import format_disk_space
-from tcia_utils.utils import remove_html_tags
-from tcia_utils.datacite import getDoi
-from IPython.display import HTML
 
 class StopExecution(Exception):
     def _render_traceback_(self):
@@ -27,10 +20,10 @@
 
 
 _log = logging.getLogger(__name__)
-logging.basicConfig(
-    format='%(asctime)s:%(levelname)s:%(message)s'
-    , level=logging.INFO
-)
+#logging.basicConfig(
+#    format='%(asctime)s:%(levelname)s:%(message)s'
+#    , level=logging.INFO
+#)
 
 
 def log_request_exception(err: requests.exceptions.RequestException) -> None:
@@ -172,6 +165,8 @@
 
         # --- Formatting Logic ---
         if format.lower() in ["df", "csv", "html"]:
+            import pandas as pd
+            from IPython.display import HTML
             df = pd.DataFrame(data)
             if 'Series UID' in df.columns:
                 df.rename(columns={'Series UID': 'SeriesInstanceUID'}, inplace=True)
@@ -465,6 +460,7 @@
     Returns:
         Optional[pd.DataFrame]: A DataFrame containing the series metadata.
     """
+    import pandas as pd
     chunk_size = 10000
     chunked_uids = [uids[i:i + chunk_size] for i in range(0, len(uids), chunk_size)]
 
@@ -672,6 +668,10 @@
     Setting a csv_filename will create the csv even if format isn't specified.
     If `as_zip` is set to True, it skips the unzipping steps.
     """
-    # Prep a dataframe for later
-    manifestDF = pd.DataFrame() if format in ["df", "csv"] or csv_filename else None
+    # Prep a dataframe for later if needed
+    if format in ["df", "csv"] or csv_filename:
+        import pandas as pd
+        manifestDF = pd.DataFrame()
+    else:
+        manifestDF = None
 
@@ -834,6 +831,7 @@
 
     data = queryData(endpoint, options, api_url, format)
     if format == "df" and removeHtml == "yes":
+        from tcia_utils.utils import remove_html_tags
         data['description'] = data['description'].apply(remove_html_tags)
         return data
     else:
@@ -932,6 +930,7 @@
     Returns:
         Optional[pd.DataFrame]: A DataFrame containing DICOM tag metadata, or None if the query fails.
     """
+    import pandas as pd
     endpoint = "getDicomTags"
     options = {'SeriesUID': seriesUid}
 
@@ -1208,6 +1207,7 @@
     input_type informs the function which of those types are being used.
     If input_type = "manifest" the series_data should be the path to the manifest file.
     """
+    import pandas as pd
     # if input_type is manifest convert it to a list
     if input_type == "manifest":
         series_data = manifestToList(series_data)
@@ -1444,6 +1444,9 @@
             Set to 'restricted' for limited-access collections or
             'nlst' for National Lung Screening trial.
     """
+    import pandas as pd
+    from tcia_utils.utils import format_disk_space
+    from tcia_utils.datacite import getDoi
     # format series_data into df depending on input_type
     df = formatSeriesInput(series_data, input_type, api_url)
 
@@ -1591,6 +1594,8 @@
     """
     Helper function for reportCollections() to create pie charts with plotly.
     """
+    import pandas as pd
+    import plotly.express as px
 
     # Calculate the total sum of data points
     total = sum(data)
@@ -1617,6 +1622,8 @@
 
     Chart width and height can be customized.
     """
+    import pandas as pd
+    import plotly.express as px
 
     # format series_data into df depending on input_type
     df = formatSeriesInput(series_data, input_type = "", api_url = "")
@@ -1664,6 +1671,8 @@
 
     Chart width and height can be customized.
     """
+    import pandas as pd
+    import plotly.express as px
 
     # format series_data into df depending on input_type
     df = formatSeriesInput(series_data, input_type = "", api_url = "")
@@ -1807,6 +1816,7 @@
         Optional[pd.DataFrame]: A DataFrame containing the extracted DICOM tags with concatenated
         column headers, or None if no tags were found.
     """
+    import pandas as pd
     tag_summary_list = []
 
     for uid in series_uids:
@@ -1932,6 +1942,8 @@
         # Display the filtered results with IDC links
         idcOhifViewer(patient_df)
     """
+    import pandas as pd
+    from IPython.display import HTML
     # --- 1. Handle different input types ---
     if isinstance(data, list):
         # Convert JSON/list output to a DataFrame
--- tcia_utils.orig/utils.py	2026-04-06 11:51:39
+++ tcia_utils/utils.py	2026-04-06 11:56:23
@@ -1,13 +1,10 @@
-import pandas as pd
 import logging
-from bs4 import BeautifulSoup
-from unidecode import unidecode
 
 _log = logging.getLogger(__name__)
-logging.basicConfig(
-    format='%(asctime)s:%(levelname)s:%(message)s',
-    level=logging.INFO
-)
+#logging.basicConfig(
+#    format='%(asctime)s:%(levelname)s:%(message)s',
+#    level=logging.INFO
+#)
 
 def searchDf(search_term, dataframe=None, column_name=None):
     """
@@ -21,6 +18,7 @@
     Returns:
     pd.DataFrame: A dataframe containing the rows where the search term was found. Returns None if an error occurs.
     """
+    import pandas as pd
 
     # If search_term is a string, convert it to a list
     if isinstance(search_term, str):
@@ -64,6 +62,7 @@
     Returns:
     pd.DataFrame: The updated dataframe.
     """
+    import pandas as pd
     # Create a copy of df_to_update to avoid modifying the original DataFrame
     df_to_update_copy = df_to_update.copy()
     
@@ -135,6 +134,8 @@
     """
     Helper function to convert HTML to plain text.
     """
+    from bs4 import BeautifulSoup
+    from unidecode import unidecode
     soup = BeautifulSoup(text, 'html.parser')
     plain_text = soup.get_text().strip()
     clean_text = unidecode(plain_text)  # Apply unidecode to remove or replace non-ASCII characters
--- tcia_utils.orig/datacite.py	2026-04-06 11:51:39
+++ tcia_utils/datacite.py	2026-04-06 11:57:31
@@ -1,17 +1,14 @@
-import pandas as pd
 import requests
 from datetime import datetime
 import time
 import logging
-from tcia_utils.utils import searchDf
-from tcia_utils.utils import copy_df_cols
 
 
 _log = logging.getLogger(__name__)
-logging.basicConfig(
-    format='%(asctime)s:%(levelname)s:%(message)s'
-    , level=logging.INFO
-)
+#logging.basicConfig(
+#    format='%(asctime)s:%(levelname)s:%(message)s'
+#    , level=logging.INFO
+#)
 
 def getDoi(format = ""):
     """
@@ -19,6 +16,7 @@
         Returns a dataframe by default, but format can be set to CSV or JSON.
         See https://support.datacite.org/docs/api-get-doi for more details.
     """
+    import pandas as pd
     datacite_url = "https://api.datacite.org/dois/"
     datacite_headers = {"accept": "application/vnd.api+json"}
     df = pd.DataFrame()
@@ -180,6 +178,7 @@
         print("\nFound the following duplicate DOIs within records:")
         print(duplicates_df)
     """
+    import pandas as pd
     # Initialize empty lists to store relationship data and duplicates
     relation_data = []
     duplicate_data = []
@@ -300,6 +299,7 @@
     >>> df = getDerivedDois(single_doi, format='df')
     >>> print(df)
     """
+    import pandas as pd
     if isinstance(dois, str):
         dois = [dois]
 
@@ -356,6 +356,7 @@
     Returns:
     pd.DataFrame: DataFrame containing the search results with an additional 'DOI' column.
     """
+    import pandas as pd
     # get current TCIA DOIs
     datacite_df = getDoi()
 
--- tcia_utils.orig/pathdb.py	2026-04-06 11:51:39
+++ tcia_utils/pathdb.py	2026-04-06 11:58:38
@@ -1,21 +1,15 @@
-import pandas as pd
 import requests
 from datetime import datetime
-import plotly.graph_objects as go
-import numpy as np
 import logging
-from tcia_utils.utils import searchDf
-from tcia_utils.utils import copy_df_cols
 import os
 from urllib.parse import urlparse
 import concurrent.futures
-from tqdm import tqdm
 
 _log = logging.getLogger(__name__)
-logging.basicConfig(
-    format='%(asctime)s:%(levelname)s:%(message)s'
-    , level=logging.INFO
-)
+#logging.basicConfig(
+#    format='%(asctime)s:%(levelname)s:%(message)s'
+#    , level=logging.INFO
+#)
 
 base_url = 'https://pathdb.cancerimagingarchive.net/'
 
@@ -26,6 +20,7 @@
     Format parameter can be set to "df" for dataframe
     or "csv" to save it to a file.
     """
+    import pandas as pd
 
     extracted_data = []
     url = base_url + 'collections?_format=json'
@@ -73,6 +68,7 @@
     Function returns JSON, but format parameter can be set
     to "df" for dataframe or "csv" to save it to a file.
     """
+    import pandas as pd
     base_url = 'https://pathdb.cancerimagingarchive.net/'
     extracted_data = []
 
@@ -165,6 +161,8 @@
 
 
 def reportCollections(df, yearCreated=None, yearChanged=None, format=None):
+    import pandas as pd
+    import plotly.graph_objects as go
     if yearCreated:
         df['yearCreated'] = pd.to_datetime(df['created']).dt.year
         df = df[df['yearCreated'] == yearCreated]
@@ -244,6 +242,8 @@
         max_workers (int): The maximum number of concurrent download threads.
         number (int): The maximum number of new images to download. Defaults to 0 (no limit).
     """
+    import pandas as pd
+    from tqdm import tqdm
     if isinstance(images_data, pd.DataFrame):
         images_data = images_data.to_dict('records')
 
@@ -311,6 +311,8 @@
 
 
 def reportSubmissions(df, useCreated=False, format=None):
+    import pandas as pd
+    import plotly.graph_objects as go
     if useCreated:
         time_column = 'created'
     else:
--- tcia_utils.orig/wordpress.py	2026-04-06 11:51:39
+++ tcia_utils/wordpress.py	2026-04-06 11:59:03
@@ -1,18 +1,14 @@
-import pandas as pd
 import json
 import requests
 from concurrent.futures import ThreadPoolExecutor, as_completed
 from datetime import datetime
 import logging
-from tcia_utils.utils import searchDf
-from tcia_utils.utils import remove_html_tags
-from tcia_utils.utils import copy_df_cols
 
 _log = logging.getLogger(__name__)
-logging.basicConfig(
-    format='%(asctime)s:%(levelname)s:%(message)s'
-    , level=logging.INFO
-)
+#logging.basicConfig(
+#    format='%(asctime)s:%(levelname)s:%(message)s'
+#    , level=logging.INFO
+#)
 
 base_url = "https://cancerimagingarchive.net/api/v1/"
 
@@ -36,6 +32,8 @@
         ValueError: If an invalid format is provided.
 
     """
+    import pandas as pd
+    from tcia_utils.utils import remove_html_tags
     # Append custom fields to the endpoint if provided
     if fields:
         fields_str = ','.join(fields)
