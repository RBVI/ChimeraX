diff --git a/prereqs/PyQt/Makefile b/prereqs/PyQt/Makefile
index 33eceae9c..0a328f365 100644
--- a/prereqs/PyQt/Makefile
+++ b/prereqs/PyQt/Makefile
@@ -25,10 +25,10 @@ endif
 endif
 
 ifeq ($(USE_QT6),true)
-VERSION = 6.2
-PYQT_MINOR_VERSION = 3
-QT_MINOR_VERSION = 4
-PYQTWE_MINOR_VERSION = 1
+VERSION = 6.3
+PYQT_MINOR_VERSION = 0
+QT_MINOR_VERSION = 0
+PYQTWE_MINOR_VERSION = 0
 METHOD=wheel.qt6
 else
 VERSION = 5.15
@@ -38,7 +38,7 @@ PYQTWE_MINOR_VERSION = 2
 METHOD=wheel.qt5
 endif
 
-MAKE_ARGS = -f Makefile.$(METHOD) VERSION=$(VERSION) PYQT_MINOR_VERSION=$(PYQT_MINOR_VERSION) QT_MINOR_VERSION=$(QT_MINOR_VERSION) PYQTWE_MINOR_VERSION=$(PYQTWE_MINOR_VERSION) PYQT_LICENSE=$(PYQT_LICENSE)
+MAKE_ARGS = -f Makefile.$(METHOD) QT_VERSION=$(VERSION) PYQT_MINOR_VERSION=$(PYQT_MINOR_VERSION) QT_MINOR_VERSION=$(QT_MINOR_VERSION) PYQTWE_MINOR_VERSION=$(PYQTWE_MINOR_VERSION) PYQT_LICENSE=$(PYQT_LICENSE)
 
 all:
 
diff --git a/prereqs/PyQt/Makefile.wheel.qt6 b/prereqs/PyQt/Makefile.wheel.qt6
index a7220cb97..69058fdf0 100644
--- a/prereqs/PyQt/Makefile.wheel.qt6
+++ b/prereqs/PyQt/Makefile.wheel.qt6
@@ -5,29 +5,28 @@ include ../pips/Makefile.pip
 
 # PYQT_LICENSE can be "commercial" for commercial version
 PYQT_LICENSE = gpl
-PYQT_LICENSE = commerical
 REMOTE_HOST = plato.cgl.ucsf.edu:
 REMOTE_DIR = $(REMOTE_HOST)/usr/local/src/PyQt6
 
-# VERSION is overriden by Makefile
-VERSION = 6.2
-QT_MINOR_VERSION = 4
-PYQT_MINOR_VERSION = 3
-PYQTWE_MINOR_VERSION = 1
-QT_VERSION = $(VERSION).$(QT_MINOR_VERSION)
+# QT_VERSION is overriden by Makefile
+QT_VERSION = 6.3
+QT_MINOR_VERSION = 0
+PYQT_MINOR_VERSION = 0
+PYQTWE_MINOR_VERSION = 0
+QT_VERSION = $(QT_VERSION).$(QT_MINOR_VERSION)
 ifeq (,$(PYQT_MINOR_VERSION))
-PYQT_VERSION = $(VERSION)
+PYQT_VERSION = $(QT_VERSION)
 else
-PYQT_VERSION = $(VERSION).$(PYQT_MINOR_VERSION)
+PYQT_VERSION = $(QT_VERSION).$(PYQT_MINOR_VERSION)
 endif
 ifeq (,$(PYQTWE_MINOR_VERSION))
-PYQTWE_VERSION = $(VERSION)
+PYQTWE_VERSION = $(QT_VERSION)
 else
-PYQTWE_VERSION = $(VERSION).$(PYQTWE_MINOR_VERSION)
+PYQTWE_VERSION = $(QT_VERSION).$(PYQTWE_MINOR_VERSION)
 endif
-SIP_VERSION = 13.2.1
+SIP_VERSION = 13.3.0
 
-PYTHON_TAGS = cp36
+PYTHON_TAGS = cp37
 WHEEL_ABI = abi3
 
 ifeq ($(OS),Darwin)
